image sdcard.img {
	hdimage {
	}

	partition uboot {
		image = "u-boot-rockchip.bin"
		offset = 32K
	}

	partition rootfs {
		partition-type = 0x83
		bootable = "true"
		image = "rootfs.ext4"
		offset = 16M
	}
}
