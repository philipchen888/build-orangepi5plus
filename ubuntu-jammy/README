Build ubuntu-jammy sdcard for Radxa rockpi4b:

Input files: ../rootfs-ubuntu-jammy/linaro-rootfs.img

Plugin NVME SSD to PC.
./mksdcardimg.sh
Eject NVME SSD from PC.

Plugin NVME SSD to SBC
Login:
Username: linaro
Password: linaro
uname -a
lsb_release -a
sudo dpkg-reconfigure tzdata
sudo dpkg-reconfigure locales
date
sudo apt update
sudo apt upgrade
sudo apt dist-upgrade
sync
sudo shutdown -h now
Power off, power on
glxinfo | grep OpenGL
date

sudo apt install speedtest-cli libjpeg-dev pkg-config libpng-dev libpng16-16 libdrm-dev libgbm-dev libwayland-dev wayland-protocols python3-pip python3-setuptools ffmpeg mpv pandoc

speedtest-cli

git clone https://github.com/glmark2/glmark2.git
cd glmark2
git checkout tags/2023.01
./waf configure --with-flavors=wayland-glesv2  # x11-glesv2
./waf build -j$(nproc)
sudo ./waf install

glmark2-es2-wayland
glmark2-es2

aplay -l
pavucontrol  ( select card 3 rockchip-hdmi1 as default )

Chromium -> youtube.com -> search YmDNhi07_Ho

git clone https://github.com/yt-dlp/yt-dlp.git
cd yt-dlp
make
cd ../
~/yt-dlp/yt-dlp -f 'bestvideo[ext=mp4]+bestaudio[ext=m4a]' hyUhEjtlDLA -o avemaria.mp4
mpv avemaria.mp4

sudo rfkill list
sudo rfkill unblock bluetooth
hciconfig hci0
hcitool dev
hciconfig -a
